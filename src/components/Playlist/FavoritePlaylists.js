import React from 'react';
import { Typography } from '@mui/material';

const FavoritePlaylists = () => {
  return (
    <Typography variant="body1" color="text.secondary">
      Hiện chưa có playlist yêu thích nào.
    </Typography>
  );
};

export default FavoritePlaylists;
